# PocketBase version (pin for reproducible builds)
PB_VERSION=0.29.3
# Optional: SHA256 of the GitHub release zip (leave empty to skip verification)
PB_SHA256=


# Runtime variables
PORT=8080
DATA_DIR=/srv/pb